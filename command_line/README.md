* Can use with For loop

* Able to provide arguments/parameters at runtime

* AWS Glue use command line arguments for passing dynamic parameters at runtime
  * Provide fixed key-pair values in Job parameters
  * Provide key but leave value blank, and provide value at runtime using 'Run with parameters'